import { FunctionComponent, useState } from "react";

export const DisplayContainer: FunctionComponent = ({}) => {

    return (
        <div>
            <div>
                This is where to game goes
            </div>
        </div>
    )
}